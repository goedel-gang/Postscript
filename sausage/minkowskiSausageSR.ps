%!PS-3.0

%%BoundingBox: 1 1 594 841
%%DocumentMedia: A4 595 842 0 () ()
%%EndComments
%%BeginFeature: *PageSize A4
<< /PageSize [595 842] >> setpagedevice
%%EndFeature

28.3464567 dup scale

newpath
0.5 0.5 moveto
20.5 0.5 lineto
20.5 29.2 lineto
0.5 29.2 lineto
closepath

gsave
0 setgray fill
grestore

clip

/height 29.7039 def
/width 20.9903 def

width 0.5 mul height 0.5 mul translate

0.02  setlinewidth

/size 5 def
/totaldepth 5 def

/minkowski {
1 dict begin

	/depth exch def

	newpath
	2 -1 moveto
	3 1 lineto
	5 0 lineto
	closepath
	1 setgray fill

	newpath
	0 0 moveto
	2 -1 lineto
	3 1 lineto
	closepath
	0 setgray fill


	depth 0 eq not {
		gsave
		2 -1 translate
		90 1 2 atan sub rotate
		1 5 sqrt div dup scale
		depth 1 sub minkowski
		grestore gsave
		3 1 translate
		360 1 2 atan sub rotate
		1 5 sqrt div dup scale
		depth 1 sub minkowski
		grestore gsave
		360 1 2 atan sub rotate
		1 5 sqrt div dup scale
		depth 1 sub minkowski
		grestore
	} if

end
} def

/scclip {
	newpath
	3 -5 moveto
	3 5 lineto
	-3 5 lineto
	-3 -5 lineto
	closepath clip
} def

newpath
size neg size neg moveto
size size neg lineto
size size lineto
size neg size lineto
closepath
1 setgray fill

gsave
size neg size neg translate 90 rotate
size 2 mul 5 div dup scale
totaldepth minkowski
grestore gsave

size size neg translate 180 rotate
size 2 mul 5 div dup scale
totaldepth minkowski
grestore gsave

size size translate 270 rotate
size 2 mul 5 div dup scale
totaldepth minkowski
grestore gsave

size neg size translate
size 2 mul 5 div dup scale
totaldepth minkowski
grestore


gsave
size neg size neg translate 90 rotate
size 2 mul 5 div dup scale
scclip totaldepth minkowski
grestore gsave

size size neg translate 180 rotate
size 2 mul 5 div dup scale
scclip totaldepth minkowski
grestore gsave

size size translate 270 rotate
size 2 mul 5 div dup scale
scclip totaldepth minkowski
grestore gsave

size neg size translate
size 2 mul 5 div dup scale
scclip totaldepth minkowski
grestore



showpage
