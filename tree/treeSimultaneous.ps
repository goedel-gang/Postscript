%!PS-3.0

%%BoundingBox: 1 1 594 841
%%DocumentMedia: A4 595 842 0 () ()
%%EndComments
%%BeginFeature: *PageSize A4
<< /PageSize [595 842] >> setpagedevice
%%EndFeature

28.3464567 dup scale

newpath
0.5 0.5 moveto
20.5 0.5 lineto
20.5 29.2 lineto
0.5 29.2 lineto
closepath

clip

/height 29.7039 def
/width 20.9903 def

width 0.5 mul height 0.5 mul translate

0.02  setlinewidth
0 setgray

/branch {
	/extent exch def
	/fooval exch def

	gsave

	0 1 extent {
		/i exch def

		0 1 translate
		0.9 dup scale

		fooval 2 i exp cvi and 2 i exp cvi eq {
			10 rotate
		} {
			350 rotate
		} ifelse
	} for

	newpath
	0 0 moveto
	0 1 lineto stroke

	grestore
	
} def

0 -10 translate
2 dup scale

0 1 10 {
	/tempext exch def

	0 1 2 tempext 1 add exp 1 sub{
		/tempval exch def

		tempval tempext branch

	} for
} for
		

showpage
